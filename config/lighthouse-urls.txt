# Canonical Lighthouse CI audit URL set (elite-performance-contract §1.3).
# One route per line. Adding a public route without adding it here means
# it is not gated. Keep this list in sync with config/lighthouserc.json.
#
# Thresholds (config/lighthouserc.json) encode the contract's 99 floors and
# elite Core Web Vitals, but are currently set to `warn`. Flip them to
# `error` to make the gate blocking once a baseline run confirms the site
# meets the bar (contract §1.1, §1.5).

/
/helios
/blog/index-demo
/blog/article-demo
