import type { Metadata } from "next";

export { default } from "@/screens/PartnershipDeck";

export const metadata: Metadata = {
  title: "Splice Labs — Partnership Deck",
  robots: { index: false, follow: false },
};
