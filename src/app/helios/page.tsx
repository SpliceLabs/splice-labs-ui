import type { Metadata } from "next";

export { default } from "@/screens/Helios";

export const metadata: Metadata = {
  title: "HELIOS Platform",
  description:
    "HELIOS — the control plane between intent and execution. Secure multi-agent orchestration for DeFi: deterministic runtimes, compiled policy, replay attestation.",
};
