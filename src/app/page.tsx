/**
 * Phase 1 scaffold placeholder. Replaced in Phase 2 when the marketing
 * home route is migrated from src/pages/Index.tsx.
 */
export default function HomePage() {
  return (
    <main className="grid min-h-screen place-items-center bg-background text-foreground">
      <div className="flex flex-col items-center gap-2">
        <span className="font-mono text-xs uppercase tracking-[0.18em] text-accent">
          splice_labs :: next_os _
        </span>
        <h1 className="font-display text-3xl font-semibold tracking-[-0.03em]">
          Next.js scaffold online.
        </h1>
        <p className="font-mono text-xs text-muted-foreground">
          Phase 1 complete — routing migration next.
        </p>
      </div>
    </main>
  );
}
