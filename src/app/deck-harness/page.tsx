import type { Metadata } from "next";

export { default } from "@/screens/HarnessDeck";

export const metadata: Metadata = {
  title: "Splice Labs — The Harness Is the Asset",
  robots: { index: false, follow: false },
};
