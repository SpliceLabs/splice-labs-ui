import type { Metadata } from "next";

export { default } from "@/screens/BlogArticleDemo";

export const metadata: Metadata = {
  title: "Deterministic Runtimes for Cross-Chain Agent Execution",
  description:
    "Agentic systems will dominate on-chain trading volume by 2027–2029. Deterministic runtimes make agent trading auditable: replayable traces, compiled policy, one cross-chain attestation ledger.",
};
