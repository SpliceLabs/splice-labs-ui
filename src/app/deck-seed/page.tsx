import type { Metadata } from "next";

export { default } from "@/screens/SeedDeck";

export const metadata: Metadata = {
  title: "Splice Labs — Seed Deck",
  robots: { index: false, follow: false },
};
