import type { Metadata } from "next";

export { default } from "@/screens/StudioModelDeck";

export const metadata: Metadata = {
  title: "Splice Labs — Studio Model Deck",
  robots: { index: false, follow: false },
};
