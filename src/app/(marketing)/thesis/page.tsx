import type { Metadata } from "next";

export { default } from "@/screens/Thesis";

export const metadata: Metadata = {
  title: "Thesis",
  description:
    "Our thesis on governed autonomous capital—where AI agents execute within policy constraints, humans approve at critical gates, and every action is auditable.",
};
