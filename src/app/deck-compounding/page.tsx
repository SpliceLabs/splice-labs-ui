import type { Metadata } from "next";

export { default } from "@/screens/CompoundingDeck";

export const metadata: Metadata = {
  title: "Splice Labs — Compounding to Network Effects",
  robots: { index: false, follow: false },
};
